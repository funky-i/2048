angular.module('starter.search', [])

    .controller('SearchCtrl', function ($scope, $location, $http, webStorage, Restangular, ClearSession) {

        $scope.Search = function() {

        }

    })
;